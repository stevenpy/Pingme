<div class="text-center">
  <%= image_tag @ping.image.url(:medium) %>
</div>
<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete Post", ping_path(@ping), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
  |
  <%= link_to "cancel", pings_path %>
</div>